# Session handoffs

One document per working session, newest last. Each is written to be fed to a fresh session that
has no memory of the previous one.

A handoff records what the *specification does not*: which way an ambiguity was resolved, which
approaches were tried and abandoned and why, the traps already paid for, and the state the machine
was left in. The design documents say what to build; these say what happened while building it.

| Session | Covers |
|---|---|
| [2026-09-07](./2026-09-07-phase-01-foundation.md) | Phase 01 — Foundation. Complete, CI green, published to GitHub. |
| [2026-09-07](./2026-09-07-phase-02-authentication.md) | Phase 02 — Authentication and Tenancy. Complete, 101 tests, verified in a browser. |
| [2026-09-07](./2026-09-07-session-aware-public-pages.md) | The landing, login and register screens notice a session. Closes an open redirect phase 02 shipped. |
| [2026-09-07](./2026-09-07-phase-03-business-setup.md) | Phase 03 — Business Setup, **backend half only**. 234 tests. Not pushed; CI has not seen it. |
| [2026-09-07](./2026-09-07-phase-03-frontend.md) | Phase 03 — the settings screens and the onboarding checklist. **Phase 03 complete.** Still not pushed. |
| [2026-09-07](./2026-09-07-phase-04-backend.md) | Phase 04 — Services and Employees, **backend half only**. 416 tests. `dev` pushed; CI green. |
| [2026-09-08](./2026-09-08-phase-04-frontend.md) | Phase 04 — the services and employees screens. **Phase 04 complete.** `main` merged and protected, and a two-phase-old timezone defect fixed. |
| [2026-09-08](./2026-09-08-phase-05-availability-engine.md) | Phase 05 — the availability engine, **backend half only**. 538 tests. Merged to `main` with a merge commit, which ends the squash conflicts. |
| [2026-09-08](./2026-09-08-phase-05-frontend.md) | Phase 05 — the availability preview. **Phase 05 complete.** Found a phase-02 defect: the transparent refresh never fires in a browser. §7.2 carries the fix, merged into `dev` on 2026-09-09 and still unpushed. |
| [2026-09-09](./2026-09-09-phase-06-backend.md) | Phase 06 — Appointments, **backend half only**. 625 tests, the exclusion constraint proven under twenty threads, and the last stub deleted. Still not pushed. |
| [2026-09-09](./2026-09-09-phase-06-frontend.md) | Phase 06 — the six Appointments and Customers screens. **Phase 06 complete.** Verified in a browser, where a `409` was raced for real and a field error found to be arriving under a name the request does not have. Merged to `main` as part of pull request #3. |
| [2026-09-09](./2026-09-09-phase-07-notifications.md) | Phase 07 — Notifications. **Phase 07 complete and merged.** 674 tests. The outbox, the poller and the Manage Link; a documented index corrected because it would have made a required email impossible. First session in five to end with nothing unpushed. |
| [2026-09-09](./2026-09-09-phase-08-backend.md) | Phase 08 — Public Booking, **backend half only**. 720 tests, no migration at all. Two questions three handoffs had carried were put to the principal and answered: the public booking body, and how a rescheduling customer sees their own slot. |
| [2026-09-09](./2026-09-09-phase-08-booking-page.md) | Phase 08 — the `/book/{slug}` page, **one of the frontend half's two pages**. Verified in a browser: a stranger books, races a real `409`, and the email's Manage Link resolves. Found the phone refusal telling Customers to change a Settings screen they cannot reach, and fixed it. 726 tests. Committed as `d6b797d` and `9785269` by the next session. |
| [2026-09-09](./2026-09-09-phase-08-manage-page.md) | Phase 08 — the `/manage/{token}` page. **Phase 08 complete.** A real reschedule and a real cancel driven in a browser, a `409` raced on the reschedule path, and the token exclusion proven against both grids. Found that the booking page promises a confirmation email it may not send. |
| [2026-09-09](./2026-09-09-confirmation-promise.md) | The booking page's email promise, and **ADR-0007**. Built no page: fixed the defect the previous handoff left as a decision, found a third case it had called safe, and found that the name rule's stated justification is false (issue #5). 728 tests. Committed as `44f42e4` by the next session. |
| [2026-09-09](./2026-09-09-manage-page-promise.md) | The manage page's email promise, and **ADR-0008**. Built no page: took the sibling defect the previous handoff handed to a separate session that never touched this tree, confirmed its trigger link by link, and found the lookup endpoint will widen it from a race to the ordinary case. 731 tests. Cleared the two-session commit backlog. **`dev` is six commits ahead of `origin` and CI has seen none of them — the pull request is the next action.** |
| [2026-09-09](./2026-09-09-copy-states-and-the-stranded-reminder.md) | The four copy states, rendered at last, and the reminder they stranded. **Phase 08 merged** as pull request #6, ten commits, CI green at last. Built no page: found that a reschedule with no address on file leaves its old reminder `PENDING`, aimed at a time the appointment has left, and fixed it with a regression test. 732 tests. Issues #7 and #8 opened. |
| [2026-09-09](./2026-09-09-phase-09-backend.md) | Phase 09 — the AI Receptionist, **backend half only**. 793 tests, `V7`, and **ADR-0009**: a hand-written RestClient, no SDK. Wiring the tools onto shipped code found three defects in it — the Receptionist could have cancelled around the Cancellation Window, cancel and reschedule asked that question two different ways, and `lookup_appointment` would have crossed tenants. Level 3 is written and has never run. |
| [2026-09-10](./2026-09-10-the-slot-race-deadlock.md) | Issues #5, #7, #8 and #9 cleared before the phase 09 frontend half. Built no screen. The widened concurrency assertion fired on the next build and named a **production defect**: a Postgres deadlock on the exclusion constraint answered nineteen of twenty slot-race losers `500` instead of `409`. Fixed with an advisory lock — retry alone was measured and is not enough. Also aligned `appointmentCreated` onto the Classic Flow's own record before building on it. 798 tests, `dev` pushed and green throughout. |
| [2026-09-10](./2026-09-10-the-flat-allow-list-and-the-40-px-default.md) | Issues #10 and #11 cleared, and **the pull request opened and merged** — [#12](https://github.com/sanama-stack/reception-booking-system/pull/12), seventeen commits, `main` current for the first time since phase 08 and **zero open issues**. Built no screen. Both issues were `ready-for-human`, so both were decided before being written: 44 px becomes the shared default rather than an override four files remember, and the public allow-list holds paths qualified by the response that produced them rather than a flat set of key names. The second was verified against its counterfactual — the old test passes green with a planted leak the new one names. 798 tests, unchanged, because this strengthened a test rather than adding one. |
| [2026-09-10](./2026-09-10-phase-09-frontend.md) | Phase 09 — the chat panel, the transcript screens and the `aiEnabled` switch. **Phase 09 complete.** A customer booked entirely by conversation in a browser, and **level 3 ran for the first time in the project's history** — 9 tests, 0 skipped, against a live model. The hallucination control was proven against its counterfactual. **§4 is a complete register of all twenty-five defects, gaps and traps found**, including two unfiled backend defects the finished screens turned up: the prompt states the date without its weekday, and tool errors drop their field detail. |
| [2026-09-10](./2026-09-10-the-day-name-that-was-not-enough.md) | The two backend defects the finished screens turned up, filed as [#13](https://github.com/sanama-stack/reception-booking-system/issues/13) and [#14](https://github.com/sanama-stack/reception-booking-system/issues/14) and both fixed. Built no screen. **The one-line fix #13 specified did not work** — told the day name, the model went on searching the wrong Saturday — which was established by a harness that replays the loop against the real strict tool JSON twenty times for a fraction of a cent, where the corpus runs each case once and answers a coin flip. 2 of 5 became 10 of 10. 801 tests. **The level-3 corpus has not run since the prompt changed**, and `dev` is five ahead of `origin` and unpushed. |
| [2026-09-10](./2026-09-10-the-saturday-that-was-still-there.md) | The corpus re-run that G5 asked for, and **the Saturday was still there** — #13's fix was working 42 times in 50, not 10 in 10. Built no screen. Fixed by reversing the seven-day list's two fields, measured over **fifty live conversations each way**: 42/50 against 48/50, Fisher p = 0.046, and the residual filed as [#15](https://github.com/sanama-stack/reception-booking-system/issues/15). Also fixed a booking fixture that stalled one turn short of the write. **The probe harness is committed at last**, with a README on the three instruments and which of them may be quoted as a rate — because the probe said 73% and 99% where live was 84% and 96%. 801 tests. `dev` is ten ahead of `origin` and unpushed. |
| [2026-09-10](./2026-09-10-the-saturday-that-never-left.md) | **Phase 09 merged** as pull request [#16](https://github.com/sanama-stack/reception-booking-system/pull/16), eleven commits, `main` current for the first time since phase 08 and #13 and #14 closed. Then took #15 and **did not fix it**: re-measuring the baseline over 150 live conversations found the SATURDAY mode is **not gone** — it is 5 of the 8 failures, against the first row's 3 — so the issue, filed on two failures, describes the wrong bug. Also found the probe **saturates**: 119/119 on a prompt that fails 5% live, so it cannot screen this class of change at all, and fifty conversations cannot detect an improvement from 96%. **The tree is dirty and the candidate in it has zero trials.** No commit. |
| [2026-09-10](./2026-09-10-the-wrong-diagnosis-in-three-places.md) | The wrong diagnosis, corrected in the two live places that carried it — [#15](https://github.com/sanama-stack/reception-booking-system/issues/15)'s title and body, and the seven-day list's own comment. Built no screen, **called no model and measured nothing**: every number here is quoted or arithmetic. Also committed the previous session's orphaned work, which had left its own handoff untracked and `PROBE_CONVERSATIONS` uncommitted — so #15's run recipe did not work on a clean clone. Three commits. **801 is now carried for a fourth handoff without being counted, and `dev` is three ahead of `origin` and unpushed.** |
| [2026-09-10](./2026-09-10-the-gap-that-was-hiding-a-wrong-write.md) | The last test gap closed, and **the wrong write it was hiding**. Four gaps shut: phase 08's two-Employee branch, the allow-list's value half (proven against a planted leak the path half cannot see), the rate test's unrecorded weekday, and **G2** — a live model now cancels and reschedules on the authorised path, which is the counterfactual four adversarial cases never had. Closing G2 found [#17](https://github.com/sanama-stack/reception-booking-system/issues/17): the Receptionist moves an appointment to a date the customer did not name, **invents a policy to justify it**, and reports success. Two failures in seven trials — recorded as not a rate. Also counted the build four handoffs carried (**804**), reverted the unmeasured candidate, and took CI off Node 20 after nine handoffs. Merged as pull request #19. |
| [2026-09-11](./2026-09-11-the-rate-and-the-experiment-that-could-not-work.md) | **[#17] measured**, and it has a rate at last: 5 of 47 writes land on the wrong date — 10.6%, CI [3.5%, 23.1%] — with `date_from` fully determining the outcome, 42 of 42 against 0 of 8. Then a second arm found that **#15 and #17 compound**: 89.4% correct when the customer reads out an ISO date against **44.8%** for "the Monday after next", Fisher p = 3.9e-05, threshold pre-registered. **The first version of that experiment could not have worked** — a bare weekday can only name a date inside the very window the model wrongly substitutes, so the failure mode was unreachable by construction. Retracted two of the previous session's claims, including my own "the probe may work here". Also fixed a **one-in-sixteen flake live since phase 07** that CI surfaced on an unrelated push, and closed a spam pull request whose proposed fix this repo had already measured and rejected. Built no product code. Merged as pull request #20. |
| [2026-09-11](./2026-09-11-two-candidates-and-the-bound-that-was-load-bearing.md) | **The first attempt to FIX [#17], and both candidates were rejected.** Built no product code; `src/main` ends byte-for-byte as it started. An instruction at the constrained-decode point came back 44.4% against a same-day control's 46.7% (p = 0.666) — though it did establish that the seven-day clamp is real, by moving the wrong landings from before the target to beyond it. Then extending the dated list to fourteen days **made it measurably worse**: never-wrote went 16% to **42%**, `date_from` accuracy collapsed 22% to 4%, and the log's mentions of `2026-09-26` — a **Saturday one day off the end of the longer list** — went from 12 to **100**, reintroducing #13's original symptom at eight times the rate. **The seven-day bound is load-bearing**, and §3 retracts my own pre-registered argument that it was not. Also found that the candidate in #17's own body **could never have been screened as written** (§4), and that the probe is **unreachable on Mondays** (§5.2). 152 live conversations. 804 tests. The stats helper is committed at last, and validates itself against every recorded number. |
| [2026-09-11](./2026-09-11-the-guard-that-locked-in-the-drift.md) | **The third candidate for [#17], rejected and harmful — the issue parked, and phase 10 started.** No product code survives the [#17] half: `src/main` ends byte-for-byte as it started there. A required `requested_date` on `reschedule_appointment`, refusing any write that did not land on it, moved wrong writes from **28% to 44%** (p = 0.9700) and piled the landings onto `2026-09-18` — **the last row of the seven-day list** — 7 against 18, p = 0.0099. Never-wrote went *down*, which is the tell: the guard was not catching wrong writes and holding them. **It made the model commit to the drift early and then enforced it** — T25, *a guard is only as good as the intent it is given*. The endpoint was changed to **wrong writes** before any model call, because a guard's success mode is refusing rather than landing, and it proved the steadier measure: it replicated the previous control where strict landing drifted fourteen points. **§4 is the instrumentation gap** — `requested_date` was never recorded, so whether the guard ever fired is unknown and the mechanism is an inference (G11). **§5 records that [#17] is parked, not resolved**: three candidates, three rejections, two harmful, all three with the seven-day clamp implicated, and **G3 stays untickable**. 100 live conversations. **Phase 10's backend half built and green — analytics and calendar, 23 tests, the suite at 827.** |
| [2026-09-11](./2026-09-11-phase-10-backend.md) | Phase 10 — Calendar and Analytics, **backend half only**. 827 tests, **no migration at all** — analytics aggregates over tables that exist and the calendar is three reads the services already owned. Each of the three traps the phase document names is proven **against its counterfactual**: revenue is re-read after the price is edited, the rates are read over an empty range where `0` is the plausible wrong answer, and the day boundaries are read after moving the Business to Auckland, where 16:00 Tbilisi is midnight the next day. The calendar deliberately **does not** reuse the detail record — a test asserts the customer's phone appears nowhere in a week of blocks. **§3.5 is a decision the principal still owns**: revenue is filtered to one currency, so a business that switches has older revenue the summary no longer reports. §4 withdraws a rationale before it shipped — the overlap query was defended with an appointment crossing midnight, which `AvailabilityEngine` cannot produce. New gap **G12**: the indexes are unverified at scale. |
| [2026-09-11](./2026-09-11-phase-10-frontend.md) | Phase 10 — the calendar, the numbers and the sweep. **Phase 10 complete but for the index verification.** Committed two sessions' worth of orphaned work first, then found the suite **red on the tree it inherited**: a notification fixture live since phase 07 could not find a bookable slot inside twenty-four hours from a Friday afternoon — the previous fix to that same test had survived every hour of the day and was defeated by the day of the week. 827 tests, 0 failures. Built `/calendar` (day and week, duration-proportional, closures and time off as regions, AI badge, drawer, click-to-create), `/analytics` and the dashboard home. The **business-timezone requirement is proven against its counterfactual** — a browser at +04:00 drawing a UTC business's 09:00Z booking at 09:00, not 13:00. The 360px sweep **measured** `scrollWidth` on eighteen routes rather than looking at screenshots, and found a page-widening `sr-only` label no screenshot could have shown (T26). §4 says which sweeps were measured and which were counted. Pushed, **CI green on all three jobs**, and opened as pull request [#22](https://github.com/sanama-stack/reception-booking-system/pull/22) — which also closes G9, since CI is where `pnpm build` can run without clobbering a live dev server. |
| [2026-09-11](./2026-09-11-the-fix-that-was-not-an-index.md) | **Phase 10 complete — [#22](https://github.com/sanama-stack/reception-booking-system/pull/22) merged and G12 measured.** `main` is current for the first time since phase 09. Then the last box: the indexes, against a clone of the production schema holding **10 000 appointments for the target business and 21 600 across nine other tenants** — because with one business in the table `business_id` matches every row, a sequential scan really is cheapest, and a green tick would say only that the table was small (**T28**). Every analytics query is index-backed and the slowest is **1.4 ms**; no index was missing and none was added. **The phase document's own prediction was wrong** — it says a slow query is fixed with an index, and the one slow thing could not be: the calendar's overlap query has no lower bound on `starts_at`, so its work is proportional to the tenant's whole history rather than to the week on screen. 8 820 rows read to return 20 at 10 000, and at 30 000 it **abandons the index and sequentially scans the whole table**. Fixed with a bound derived from `MAX_DURATION_MINUTES` — 46x and 83x — and **`V8`**, the phase's only migration, so the ceiling the bound depends on cannot be raised out from under it. The bound test was **shown to fail when the bound was wrong** while `CalendarViewTest` stayed green throughout. 831 tests. Also retracted a claim of my own about the availability query, filed it measured as [#23](https://github.com/sanama-stack/reception-booking-system/issues/23), and quoted one number off a table full of dead tuples before catching it (**T29**). **`dev` is one unpushed commit ahead and CI has never seen the migration.** |
| [2026-09-11](./2026-09-11-the-ceiling-that-was-two-ceilings.md) | **Phase 10 merged** as pull request [#24](https://github.com/sanama-stack/reception-booking-system/pull/24), six checks green — `main` current at last. Then took **[#23](https://github.com/sanama-stack/reception-booking-system/issues/23)** and fixed it: the availability engine's read, which runs on every page of the booking flow, is bounded on both sides and now reads **13 index entries where it read 11 164**. **The constraint the issue specified would not have made the bound safe** — a single check on the total blocked span is satisfied by a long leading buffer paired with a short duration, which still breaks the second bound, so `V9` holds each buffer separately. Measured on **Hibernate's own generated statement** under a generic plan, which is what G15 said the calendar fix never was; the issue's headline 46x survives as the *buffer* ratio, not the time one. Both bounds shown to fail when narrowed, while 265 existing tests stayed green. 837 tests. **Merged as pull request [#25](https://github.com/sanama-stack/reception-booking-system/pull/25)**, six checks green, and `dev` left byte-for-byte identical to `main` — the first session since phase 07 to end with nothing outstanding. |
| [2026-09-11](./2026-09-11-the-first-candidate-that-worked.md) | **Eight decisions recorded, [#26] fixed, and the fourth candidate for [#17] measured — the first one that worked, and it is not accepted.** The decisions had each been carried as a question rather than a task; the revenue currency was **six handoffs old** and three sessions had re-explained it. **ADR-0010** takes it. Two entries are corrections: phase 11's *"any miss is fixed with an index"* is **struck** on phase 10's own evidence, and `08-testing-strategy.md` §11's Frontend row is **widened rather than ignored**, because a frontend test runner contradicts a decision phase 02 recorded as deliberate. Then [#26]: `lookup_appointment` returned `service_name` while `find_available_slots` requires the id, so every reschedule had **the model** match a name back to an id over a catalog a Business can fill with near-identical names — #17's class of defect with a two-line fix. **The level-2 test the issue specified could not have worked** (T37): the script chooses the tool calls, so it would assert its own arrangement. Then [#17], pre-registered before any model call: `resolve_date` moves the date arithmetic into code. Two arms of fifty — the primary went **18% → 58%, p = 3.5e-05**, the customer-visible rate **40% → 81.6%**, and *other reading* collapsed 24 → 1. **A pre-registered veto fired**, and the harness could not say whether it should, because it logged one tool and the veto turned on another — **G17**, the same blindness as G11 one candidate later. G17 was closed in the harness and **the deciding arm stopped at fifteen trials when the OpenAI account ran out of credits**: GAVE 6, NEVER GAVE 0, suggestive and not a verdict. The outage also nearly produced a false finding — the summary read **"35 of 50 never wrote"** for a run where no model was reached at all (**T36**). 844 tests. **`dev` is three ahead of `origin` and CI has seen none of it.** |
| [2026-09-11](./2026-09-11-the-gate-that-would-have-ticked-itself.md) | **[#17]'s standing settled, and the MVP Definition of Done audited — it was a phase-01 scaffold.** Built no product code; `backend/src` and `frontend/src` end byte-for-byte as they started. The pre-phase-11 survey's headline finding was **wrong in direction** and §2.2 retracts it: phase 11's gate was not blocked by [#17], it was **reachable in spite of it** — `07-mvp-scope.md` has exactly one commit against it, `aa84b19` from phase 01, and its Receptionist rows are weaker than phase 09's, which phase 11 never references (**T39** — a gate is only as strong as the oldest document it points at). The principal chose to correct the scaffold rather than gate the MVP on an unfunded experiment: **five** rows widened, not three, and **two of the five have nothing to do with [#17]** — which is the argument for walking all thirty. [#17] is now carried as an **accepted, measured, open defect** with its rate, under a rule that a box is ticked against evidence or listed there, with no third state. Then two clean-clone defects: **`.vscode/` was gitignored while the README documented `F5` as the way to run the system** (**T40**, and the negation that fixes it must use `dir/*` plus `!`), and two documents published container-internal ports. The README build status was a phase behind **and claimed the receptionist "cannot invent a policy"** — measured false at 10.6%; the claim is narrowed and the retraction written in. New gap **G18**: `reception_perf` is unmigrated and missing the V8/V9 ceilings its bounds depend on. **Three commits, `dev` is seven ahead of `origin` and CI has seen none of it.** |
| [2026-09-11](./2026-09-11-the-headers-that-existed-only-in-prose.md) | **The pre-phase-11 survey, and the backlog it said was the biggest problem — shipped.** Built no product code; `backend/src` and `frontend/src` end byte-for-byte as they started. The survey's headline was the **release state**: eight commits unpushed, **385 insertions in the AI module CI had never compiled**, and an `origin/main` still saying "phase 09 of 11" and claiming the receptionist "cannot invent a policy" — measured false at 10.6%. Pushed, opened [#27], six checks green, **merged as `fb9717a`** with [#26] closing on it, and `dev` fast-forwarded so both branches hold an identical tree. Then four drifts nobody had filed, of which one matters before phase 11 starts: **`06-security.md` §13 documents an HSTS and a CSP that exist in no file** (**G20**), and phase 11's header test — specified against Caddy — would go green and certify the gap (**T42**, the mirror of T39: decide, then assert). Also the tool count says eight in three documents and nine in one, and its resolution waits on [#17]'s unfunded verdict. Two phase-11 items are **design work, not typing**: the E2E's scripted model is test-classpath only and cannot be reached by a booted app (**G19**), and the reflection isolation suite's cost is the per-endpoint fixture, not the reflection. **G18 is worse than filed** — `reception_perf`'s `flyway_schema_history` exists and is *empty*, so Flyway would treat a populated database as unmigrated. The suite was **re-run, not carried**: 844, 0 failures — after `./gradlew test` first printed `BUILD SUCCESSFUL` in seven seconds having run nothing, and the XML count returned a full green from stale files (**T41**). |

| [2026-09-11](./2026-09-11-the-seed-and-the-headers-that-existed-at-last.md) | **Phase 11 started: the two-tenant seed, and the two security headers that existed only in prose.** `make seed` writes Salon Aria (`Asia/Tbilisi`, GEL) and Dato's Auto (`Europe/Berlin`, EUR) through the same application services the dashboard calls — appointments excepted, because **the engine cannot book the past** and half the data is the history that makes Analytics show anything. Placement is anchored to the **week**, never to a date, so a Wednesday booking is a Wednesday booking whenever it runs. `BlueprintCheck` catches the class of defect the application would happily store — a booking outside opening hours is *legal* — and is **proven able to fail against six planted defects** in the real blueprint. Both tenants verified in a browser, and the 890.00 GEL on the analytics card checked by arithmetic. Then **G20 closed**: the CSP and HSTS `06-security.md` §13 has claimed since the design phase now exist, with `'unsafe-inline'` on `script-src` **named as a concession** rather than implied away, and `make check-headers` written against the **running origin** rather than the Caddyfile — **made to fail before it was trusted**. `next dev`'s need for `'unsafe-eval'` was **measured in a browser console**, not assumed. The **demo script was walked and was wrong** (T44): its default slot lands inside the 24-hour Cancellation Window, so the step proving the Manage Link works dead-ends. Also **T43** — a `.env` value that must carry its own quotes loses one layer to Compose, and the policy then names a *host* called `unsafe-eval`. Four documentation drifts closed, including a `Manage Link` default pointing at a container-internal port. Then the **performance dataset**, which closes **G18, G15 and G16**: a committed generator that **migrates rather than clones** — the old scratch database was a schema clone taken before `V8` and `V9`, so it was missing the three CHECK constraints the bounded queries depend on, and the script's assertion of them was **run against that clone and named all three** before it was dropped. All three NFR checks pass with room to spare, measured **through the application services** so the statement is Hibernate's and the parameters are bound. **G15 corrects a number this project has been quoting**: the calendar bound is 46x as a ratio of queries and **1.2x** as a ratio of operations, and neither is why it exists — the unbounded query's cost is a slope, not a factor (**T45**). No model was called; the account is still out of credits. **Seven commits, CI green on `dev` twice** — the one red run was Docker Hub resetting a pull, not a change — and `main` deliberately left where it was: whether these go in now or wait for more of phase 11 is the one decision this session did not take. **Taken 2026-09-12 — merge**: pull request [#28](https://github.com/sanama-stack/reception-booking-system/pull/28), eight commits, merged as `34c454e` with `dev` fast-forwarded onto it. |

| [2026-09-12](./2026-09-12-the-model-that-had-to-read-the-transcript.md) | **The merge the previous session left open, and phase 11's next item designed and half built.** Built no product code; `backend/src` and `frontend/src` end **byte-for-byte** as they started. First the merge: pull request [#28](https://github.com/sanama-stack/reception-booking-system/pull/28), eight commits, `34c454e`, `dev` fast-forwarded onto it — and a first `gh pr merge` **refused with a cause that was not the cause** (*"the base branch policy prohibits the merge"* while `mergeStateStatus` read `CLEAN` and zero approving reviews were required), where the obvious response is `--admin` and would have bypassed a protection that was not blocking anything. Then **G19**, which turned out not to be an override at all: phase 11's *"against the scripted model"* reads like a ruling but names a class on the test classpath that a booted application cannot reach — **T46, a specification sentence is not a decision, and the two are written identically**. Reopened, put to the principal, and answered as **[ADR-0011](../adr/0011-the-e2e-fake-provider-lives-behind-the-base-url.md)**: the E2E's model lives behind `app.ai.base-url`, so `OpenAiChatModel` stays on the path instead of becoming the one class no end-to-end test crosses. The provider **cannot be a tape** — a booking names ids `make seed` regenerates every run — so it answers from the transcript it is sent, the mirror of what `probe.py` does from the other side. **Proven twice**: 19 self-test checks **shown able to fail** against four planted defects, and a real Appointment booked against the real seeded tenant reading **`source = AI`**, which is the assertion that rules out a silent degrade. **T47** is why that matters — an absent API key does not fail the Receptionist, it **degrades** it to the Classic Flow, so an E2E with no key books through the wrong door and looks green. **T48**: the turn-counter plant was caught by **only one** of the two cases meant to catch it, because the obvious transcript coincides with a counter's state. Also retracted **my own comment's** claim that two days is safe in any timezone (it is not, at `+14:00`), and fixed a `make help` pattern that **hid** `up-e2e` rather than listing it wrongly. CI green on `dev`, and the new step checked for having *run* rather than for the job being green. New gap **G22**: the topology is configuration-verified but has never been started. |

| [2026-09-12](./2026-09-12-every-failure-was-the-test.md) | **The Playwright flow, green in CI — and not one of its eleven red rounds was a defect in the application.** Built no product code; `backend/src` and `frontend/src` end **byte-for-byte** as they started. Every step of `08-testing-strategy.md` §8 now runs in a browser: register, configure, book classic, **book by conversation**, the confirmation email in Mailpit, the Manage Link followed and cancelled, the owner back in, one booking badged `AI` on the calendar, marked completed, analytics. Plus the 360 px run. **The flow found no bugs, and §3 records that as the finding** — every failure was a locator naming the wrong thing, a wait asserting the wrong condition, or a consequence of an earlier choice of mine. **T49**: an ambiguous Playwright locator does not fail slowly, it **throws**, silently cancelling the very timeout meant to let a page settle — three rounds went that way, and `.first()` is not the fix, because it resolves ambiguity but not wrongness (round 10 picked a `<select>` option nineteen times). **T50**: a `waitForURL` timeout can mean *navigated somewhere else* — the customer's name is a link, to `/customers/{id}`. **T51**: booking the fortnight's **last** day, chosen so the cancel step clears the Cancellation Window (T44), then broke two later steps that knew nothing about it — the calendar opens on today, and **every** analytics preset ends at today, so a future booking could never move revenue. Also replaced a `toBeGreaterThan` that would have passed on any number with the exact figure. **No browser can run on this machine**: a network sandbox allows `playwright.azureedge.net` but not `cdn.playwright.dev`, so every install extracted 624K and `--dry-run` called it installed — CI was the only place this could run, hence fifteen commits. **G19, G22 and G21 closed**, G21 because a real browser has now driven hydration, forms and the chat panel against a **production build under the strict CSP**, which no `curl` check could ever have shown. New gap **G23**: the End-to-end job is **not** a required status check, so it can go red without blocking a merge. |
| [2026-09-12](./2026-09-12-the-probe-that-only-looked-one-way.md) | **The isolation suite, built classify-or-fail — and the one plant that did not go red is the finding.** Built no product code; `backend/src/main` and `frontend/src` end **byte-for-byte** as they started. Reflection over `RequestMappingHandlerMapping` lists the endpoints; a hand-written catalogue says what isolation *means* for each; and the gate fails **in both directions**, so a new controller method breaks the build until somebody classifies it and a stale entry cannot make the catalogue look complete. Then three sweeps: **27** id endpoints handed Datos Auto's real id and required to answer `404`, **24** reads asked twice and required to answer **byte-identically** with `?businessId=` appended, and **12** public endpoints probed for their own shapes, because a Manage Link carries its own authority and there is no id to substitute into a slug listing. **Every probe runs a control first** — the same request with the caller's own id, required to resolve — since `404` is also the answer to a mistyped path, a wrong method and a rejected body (**T54**). Five plants, and **the fifth exposed the probes rather than the application**: resolving every slug to whichever Business sorts first left six of seven listing probes **green**, because Salon Aria registers first and every probe inspected Aria's page only — proving Aria was not the victim, which is not the same thing (**T52**). Every listing probe now asks both pages; the same plant then turns six red. **T53** is the other half: unscoping `ServiceCatalogService.read` did **not** surface at `GET /services/{id}`, because `AssignmentService.employeesFor` checks the tenant again on the same request — a plant that fails to go red may mean the system is stronger than the plant. Three response shapes were wrong in the first draft and are now pinned, including that a failed lookup is **`401` by design** — a proof that does not hold, not a row being hidden. **928 tests, 0 failures**, full suite run twice. Ten phase-11 boxes, two of them ticked on work that already existed and was **read before being ticked**. New gap **G24**: the body half of the smuggling sweep observes two endpoints; the rest rest on `TenantRepositoryShapeTest`'s compile-time rule. **Taken the same day — push and widen**: PR [#30](https://github.com/sanama-stack/reception-booking-system/pull/30) now carries 23 commits and both halves. The push turned **End-to-end red at a commit that had already passed**, which is the shape of a flake and was not one: the run's own artifact showed the range holding one appointment, the tile reading `0.00 USD` and the appointment still `CONFIRMED` — the mark-completed click had never landed, and the step meant to catch that asserted `getByText(/completed/i)`, which **matches the "Mark completed" button itself**. **T49 recurring two sessions running, in code written by the session that wrote T49 down.** Fixed by asserting what the trigger cannot satisfy — the badge reading exactly `Completed`, and the action block, rendered only while `CONFIRMED`, being gone — and `revenue()` now returns `null` rather than `0` for an unreadable tile, since returning 0 made "not rendered yet" and "no revenue" the same value. **T55**: a red job on a commit that already passed is evidence of nondeterminism, not of a flake — reach for the artifact before the word. The previous handoff's two §10 confidence claims now carry dated corrections. Then **G23 closed — before the merge, deliberately**, so `main` gained the gate while there was still something to gate: `End-to-end` joins the three required checks with `strict: true`, written through the `required_status_checks` **sub-resource** rather than the full `PUT` that silently resets every field it is not given, and the whole protection read back afterwards and compared. **Merged**: [#30](https://github.com/sanama-stack/reception-booking-system/pull/30), 24 commits, **`a71d17a`**, a merge commit as #28 and #29 were, **no `--admin`**. `main` and `dev` level. The gate was **satisfied, not exercised** — the first merge `End-to-end` can actually block is the next one. |

| [2026-09-12](./2026-09-12-the-pixel-that-jsdom-could-not-see.md) | **The frontend test runner — and the one target that could not be built where the phase asked for it.** Built no product code; `backend/src` and the non-test half of `frontend/src` end **byte-for-byte** as they started. Vitest 5 and Testing Library, **18 files and 68 tests**, wired into CI's Frontend job — closing the gap carried since phase 02, where the absence of a runner was recorded as deliberate. **The business-timezone row is done and proven as specified**: the suite runs at `Asia/Tbilisi` against **UTC** businesses, and the calendar asserts both halves, because there the zone decides pixels as well as text — the block is labelled `09:00` *and* drawn 60 px down, not at the 300 px the browser's own reading would give. Each file asserts the counterfactual is **in force** first, and **CI is what proves that guard is not decoration**: GitHub's runners are UTC, so a `TZ` that failed to reach the worker would have gone red rather than passing against a business and a browser that happened to agree. Shown able to fail — a planted browser-zone fallback turned both assertions red. **Loading and error cover every screen** (they come from `ResourceGate` everywhere but `DetailDrawer`, which hand-rolls both because they must sit inside the dialog under a heading and above a Close button); **empty covers fifteen of nineteen**, the other four **counted** in `UNASSERTED_EMPTY_STATES` so a fifth turns the gate red. A **classify-or-fail gate** over both, mirroring `EndpointCatalogue`: discovery finds every component reading a resource or rendering an empty state, the catalogue judges each, and it fails **in both directions** — every literal `EmptyState` title is read out of the source and must appear in a test file the entry names. Proven by two plants. **Then the row that could not be built**: phase 11 asked for the 360 px widths in the frontend runner, and **jsdom has no layout engine** — a `div` explicitly 1200 px wide reports `scrollWidth`, `offsetWidth` *and* `clientWidth` of **0**, so the natural assertion reads `0 <= 0` and passes forever, a vacuous assertion of exactly the kind T49 and the mark-completed step already cost this project. Put to the principal **with the measurement**, and answered: extend the Playwright `mobile` project, which grew from one route to **twenty-one**, green, every one `360/360`. **And then it passed in 3.9 seconds** — for a registration and twenty-one page loads — and the log could not settle whether it had swept anything, because the `list` reporter prints the test and not its steps. The time was honest; a green tick could never have shown that (**T59**: a plant proves a measurement *can* fail and says nothing about whether it was *taken*). It now prints every route's numbers and asserts how many it measured. Also **T56** — Vite 8 transforms with Oxc, so an `esbuild: { jsx }` option is **silently** ignored and the error blames `tsconfig.json`'s `jsx: preserve`, which is correct; **T57** — jsdom implements `<dialog>` but none of its modal behaviour; and **T58** — no CSS is loaded in a unit test, so `toBeVisible` means *in the document*, **not** *a person could see this*. New gap **G25**: the five id-taking routes are outside the sweep, and they are the ones that render tables. Nine commits of work plus this handoff, **all four CI jobs green** on three runs, `main` ten behind and no PR opened. |
| [2026-09-12](./2026-09-12-the-test-that-needed-the-id-to-be-missing.md) | **The last four empty states and G25 closed, and a defect found in the test suite itself.** Built no product code. **Pull request [#31](https://github.com/sanama-stack/reception-booking-system/pull/31) merged** — ten commits, and the first merge the `End-to-end` gate actually exercised rather than merely satisfied. `UNASSERTED_EMPTY_STATES` is **zero**: all nineteen screens now assert empty, loading and error. Closing them found that `useParams` answered `{}` for every test, so a `[id]` page requested `/appointments/undefined` — which the prefix-matching harness answered, meaning the test passed **because the id was missing** and would have gone on passing had the page stopped reading its route (T60, T49's family from a third side). The 360 px sweep grew 21 → **26 routes**, every one `360/360`, the five id-taking routes among them — and its runtime moving 3.9s → 5.4s is what the count assertion is for. The session's reusable lesson is **T61**: ten green `curl`s proving five endpoints answer `200` established the *requests* and nothing about the *measurement*, so the G25 row was left open until CI printed twenty-six. 22 files, 76 tests. |
| [2026-09-12](./2026-09-12-the-audit-that-found-nothing-in-the-file.md) | **The deployment path written at last, and an audit whose finding was not in the file it audited.** Built no product code across two merged pull requests — [#32](https://github.com/sanama-stack/reception-booking-system/pull/32) (the previous session's five commits) and [#33](https://github.com/sanama-stack/reception-booking-system/pull/33) (three) — and `main` moved twice, ending **level with `dev`**. `docs/deployment.md` resolves two dangling references and **opens by saying no host has ever run it**, naming three files that cannot deploy as committed: a Caddyfile that issues no certificate and does not error, a compose file publishing Postgres and Mailpit on `0.0.0.0` below `ufw`, and Mailpit, which delivers nothing. The `.env.example` audit found the file **complete in both directions** and one sentence wrong — the real finding was outside it: **four ports are written down twice and `make check-ports` guarded one**, a gate that had never run in CI because CI invokes `up-all`, not `up`. All four guarded now, each shown red on a planted half-move. And **a code reading that looked like a security hole was refuted by measurement**: forging `X-Forwarded-For` buys no rate-limit bucket — established only by the second probe, since one source address cannot tell correct per-IP limiting from a collapsed global one. **G26** filed: nothing checks a documented control against its absence. 43 of 72 phase-11 boxes. |
| [2026-09-12](./2026-09-12-the-machine-that-could-verify-after-all.md) | **`ai_message` retention built — the oldest carried item, and the only open one touching data the system keeps about real people.** `V10`, `TranscriptPurge` and an hourly `TranscriptPurgeJob`: ninety days after a conversation's **last activity**, its transcript is deleted. Anchored on the conversation rather than on each message, because a per-message cutoff splits a straddling conversation into the half-scrubbed transcript phase 11 rejected. The `ai_conversations` row is **kept and marked** (principal decision) for its cost accounting — which forced a screen change, since `message_count` is not decremented and the transcript's empty state would otherwise have claimed a full conversation was silent, under a Rows count contradicting it. Nine backend tests, every deletion paired with a survival, **shown red three ways**. And **[the previous handoff](./2026-09-12-the-audit-that-found-nothing-in-the-file.md) was wrong that this machine could not verify it**: T62 is about the containerised stack, the suite uses Testcontainers, and all three images were already cached — 937 tests ran locally. **G26 gains a third instance**: `AI_RETENTION_ENABLED=false` turns the purge off and nothing would say so. Then, on the principal's call that **the file moves rather than the sentence**, `06-security.md` §12's claim that the database port is local-only was finally made true: Postgres and Mailpit bound to **loopback**, the deployed topology publishing **no database port at all**, and `make check-bindings` holding all three topologies — shown red against the `ports: []` that looks like it removes a mapping and, because Compose appends sequences, does not. Measured with the old binding answering on the LAN address as the control. Finally the **docs consistency pass**, done as `make check-docs` rather than as an event — four mechanical checks with their own CI job, including **265 `§N` cross-references that are cited from `.java`, `.yml` and `.sql`**, each check failing if its population reaches zero. It found `GET /calendar` missing from the API surface since phase 10, an architecture paragraph contradicting itself within one sentence, and **four terms the design docs use ~220 times that the binding glossary never defined**. Phase 11's Documentation section is **closed**. 47 of 72 boxes. **Merged** — PR [#34](https://github.com/sanama-stack/reception-booking-system/pull/34), `main` at `6d01bf8`, five required checks green. And the lesson the session ends on: making the new docs job *required* meant reading branch protection for the first time, which revealed that **a comment written two commits earlier claiming the job was already required was false** — G26 committed into the workflow file that structurally cannot enforce it, in the session that filed G26. No gate caught it. |
| [2026-09-12](./2026-09-12-the-controls-that-had-never-run.md) | **Three security controls that had been read, reviewed and shipped, and never once executed.** `SecretsGuard` is `@Profile("prod")`, so no test in the suite had ever constructed it — written in phase 01, unexecuted for ten phases, and `deployment.md` §5 had said so in as many words. The redaction was tested at the masker, at the converters and at the appender's *type*, and **not one of those tests notices when the masking is deleted from the appender that runs in production** — demonstrated as a plant, not argued. And §11's leakage claims had only ever been asserted against a `404` from the dispatcher, which never had a stack trace to give away. Twenty-four tests across three classes, **shown red seventeen ways**. The plants earned their keep: a local default in `application.yml` that loses its `local-dev-only-` prefix **silently disables the guard** with every other test still green; **Boot's own `defaults.xml` binds `wEx` too**, so the include order in `logback-spring.xml` is load-bearing and looks like formatting; and deleting the error catch-all **does not leak, it lies** — the client gets `401`, because the servlet error dispatch is not in the permitAll list (predicted wrong here, corrected by capturing the body). Also found the previous audit's correction had been applied to **one file of five**. But the finding that outlives the tests is the environment: **`make check-bindings` was green while both databases answered on the machine's LAN address** — the containers predated the change, and a gate that reads `docker compose config` describes the next `up`, never the running one. **G27** filed. Six phase-11 rows ticked, 53 of 72. **Nothing pushed** — `dev` is 5 ahead of `main` and CI has seen none of it — and the **E2E stack is down**, three rebuilds defeated by a tethered connection. |
| [2026-09-12](./2026-09-12-the-empty-set-that-passed.md) | **Six rows, and the same failure twice: a derivation that silently produced nothing, and an assertion that passed because there was nothing to check.** Both caught by a positive control rather than a plant — T89 arriving twice more. "Every public endpoint is rate limited" had been checked against a **list of ten paths somebody typed**, wrong since phase 09 because neither chat endpoint was ever added to it. Derived from `RequestMappingHandlerMapping` and the **`AuthorizationManager` inside the running security filter chain** instead, it found **three endpoints that had never carried a limit** — `/auth/refresh`, `/auth/logout` and `/health`, the last of which opens an outbound SMTP connection for anyone who asks. Then the observability block: the health endpoint's **only test could not fail** (each dependency now fails against a real refused connection, and every failing case asserts the *other* is still `UP`); request id and `business_id` were in the MDC with **nothing connecting them to a log line**, including the public tenant filter no test had ever touched; **LLM call metrics did not exist at all** — the loop had no log statement about a model call and the cost the cap was charged was discarded after checking, now emitted as structured fields with two plants that deliberately leak content to prove §10; and slow-query logging was **unconfigured**, its first test flaky by construction because a one-millisecond threshold is met by statement preparation exactly once. Twenty-three plants, all reverted and `cmp`-verified. 985 tests. **Phase 11 at 59 of 72.** Two commits, still unpushed — CI has now not seen three sessions of work. |
| [2026-09-13](./2026-09-13-the-controls-that-kept-passing.md) | **Four sections of the security walk, three of which described a control no file implemented — and three times the session's own controls passed a plant before they were fixed.** **§6** is the finding: *"Manage tokens are excluded from access logs"* was true of nothing, and Caddy — the only access log in the system — wrote every one to stdout verbatim, as a path segment in `/manage/{token}` and as a query parameter on the two API calls that page makes. A Manage Link is a bearer capability good until 24 hours after the appointment ends, so the log was a file of live credentials. Measured against the running container, not read off the Caddyfile. **The error logger was the trap**: a site's `log` directive filters `http.log.access` only, so the first fix passed against a healthy stack and leaked every token the moment the backend was down — found by probing with it stopped. **§2**: `RegistrationTest` asserts three cookie attributes over real HTTP and *structurally cannot* assert `Secure`, because the suite runs the `test` profile where it is deliberately off; `application-prod.yml` set to `secure: false` left everything green. **§13**: *"state-changing requests additionally require `Content-Type: application/json`"* held wherever a `@RequestBody` existed and was false at the ten endpoints that take no body — a form-encoded `POST /auth/logout` answered **204**. Not exploitable, because `SameSite=Lax` carries it, which is exactly why it mattered. **§7**: three bare strings on two unauthenticated endpoints, each with a bounded twin a few lines away, and no global request-size cap to fall back on. **But the part worth reading is §6.1.** Two of three plants *passed* against the test as written: `@Size` is not readable from a `RecordComponent` (the first derivation reported **all 59 fields unbounded and none bounded**, and written the other way round would have passed forever), and a walk that stops at nested records drops **the one record the class exists to catch**. Both are closed by naming a *nested* element in the *bounded* set. The sharper rule: a positive control must assert the specific thing whose absence would make the main assertion vacuous — the deepest element, not the easiest. Also the carried **revenue remainder** (ADR-0010), built with the expired test deleted rather than patched. 999 backend tests, 82 frontend. `make check-access-log` is new and in CI's compose smoke — where it has never run, because **nothing is pushed**: 14 commits, four sessions, and the principal deferred the push deliberately. Phase 11 at **61 of 72**. |
| [2026-09-13](./2026-09-13-the-tests-that-could-not-see.md) | **Four more sections of the security walk, and three times the defect was the instrument rather than the code.** **§14**: *"Authentication events are logged with user id and IP"* — `AuthService` contained **no log statement at all**, and the only authentication line in the application was a replay warning carrying a user id and **no address**. The trail began at the one event an attacker triggers deliberately. Logged now, registration included, the events **derived** from the `/auth` surface. **§8**: *"business text is delimited and labelled as data"* was true of **one field of four, and it was the smallest** — `ai_additional_info` fenced at 2,000 characters while the description, the cancellation policy and fifty FAQs went in bare, some thirty times as much owner free text, and the FAQ is the field the architecture doc's own injection table names as an attack. **Neither of §8's claims was checked by anything that runs**: the only test touching `SystemPromptBuilder` is `@Tag("probe")`, excluded from the suite. **§13's CORS claim** was true and asserted by nothing; checked in two halves, because a `@CrossOrigin` naming one partner is invisible to any probe. **§3 and §4**: `EndpointCatalogue` is the antidote to §5's failure, not an instance — but **classified is not probed**, and fifteen of sixty-five endpoints carried a written probe description no test referenced. **The plants are the part worth reading.** A preflight assertion **passed against an application granting every origin everything with credentials**, because `Origin` is a restricted header for `HttpURLConnection`. A sweep **reported green over the entire authenticated surface**, because Spring Security answers `401` before MVC runs. A credential sweep **passed a password written into a log line**, because it skipped the one endpoint that receives a password. A fence emitted where the text is not **passed any search for the marker**. And one stayed green because two independent tenant checks guard the same id — §4's "each independently sufficient" demonstrated rather than asserted. 1024 backend tests, 82 frontend. **§8 changed the system prompt and the level-3 corpus has not been run against it** (G32). **Still nothing pushed**: 23 commits, five sessions, no CI. Phase 11 at **61 of 72**. |
| [2026-09-13](./2026-09-13-the-control-held-by-a-coin-toss.md) | **§1, the threat-model table — and the first section of the security walk that was very nearly clean.** Seven rows name seven primary controls and **six already resolved to a test that runs**; after three sessions of finding claims no file implemented, the walk confirmed the work rather than corrected it. **The seventh is the finding**: *"Rate limiting by IP"*, the control for the **availability** row, was **true and its *by IP* half was asserted by nothing.** Two tests look like coverage — one proves every public endpoint is matched by a policy, the other proves a limit bites — and **both drive a single client**, so both are equally true of a filter keying every bucket on a constant. The property is claimed four further times in `RateLimitProperties`' own prose and checked in none. It rests on **three independent facts**, any one undoable with the suite staying green: the `forward-headers-strategy` line, Spring registering `ForwardedHeaderFilter` **ten ahead** of `RateLimitFilter`, and `clientAddress()` reading `getRemoteAddr()` — the last two read off the **bytecode**, because had the third been false this was a live production defect rather than an unasserted control. The failure they admit is the **inverse** of the control, which is why the row is *Availability*: one bucket for every visitor lets a single stranger close a public endpoint for everybody. **No production code changed.** **But the part worth reading is the fourth plant.** Moving `RateLimitFilter` to `HIGHEST_PRECEDENCE` — the obvious edit for a filter that must precede authentication — ties the two, and **the behavioural test stayed green**: tied filters are sequenced arbitrarily and that run landed the right way. Only the assertion on the registered orders caught it. Not an instrument that was blind, like the last three sessions', but one that was **sampling** — where correctness depends on an ordering nobody declared, probing the outcome reports the coin, not the rule. 1027 backend tests, 116 classes. **Still nothing pushed**: 25 commits, six sessions, no CI. **§15 is the last section of the walk.** Phase 11 at **61 of 72**. |
| [2026-09-13](./2026-09-13-the-table-nothing-could-test.md) | **§15, the accepted-risks table — and with it the security walk is complete, fifteen of fifteen.** §15 is the one section that **cannot be asked the walk's question**: the other fourteen name controls and the test is whether each resolves to something that runs, but an accepted risk resolves to nothing *by definition* — the entry **is** the decision not to build the control. So it is checked for the opposite defect, an entry that no longer describes the system, and **three of seven were wrong, each differently.** A **justification that was false**: *"the account model supports adding 2FA without migration"* — `users` has six columns, none able to hold a secret or an enrolment flag, and no credentials table exists, so a reader was told the wrong price for reversing the decision. An entry that **recorded half its risk**: the API documentation row had the disclosure and not the **amplification** — `/openapi` hands any anonymous caller the full specification, ~45 KB, and **no policy matches it**; two hundred consecutive requests, none refused, measured. It went unnoticed because **every derived control in this repository filters to `dev.reception`** — deliberately, so a springdoc rename cannot break the build — and these paths are springdoc's: anonymous, unlimited, and **invisible to every sweep in the tree**, with Caddy's `handle /api/*` proxying all of them. And a **risk that was missing**: the Manage Link is a bearer capability in a URL, discussed at length in §6 as a fact about the token and never carried here as a decision. `ApiDocumentationExposureTest` pins the row's live claims so that **closing** the risk cannot happen silently. **Shown red three ways, and the third is the lesson for the third time in this walk**: with the limiter switched off, *"the documentation is unlimited"* **passed** — vacuously, since two hundred requests are also unrefused when nothing limits anything — and only the positive control caught it. An absence assertion is always true of a probe that has stopped working. **No production code changed**, for the second session running. Closing the exposure is left as an open decision (G34). 1030 backend tests, 117 classes. **Still nothing pushed**: 27 commits, seven sessions, no CI — and with the walk done, **eight of the ten remaining phase-11 items want the E2E stack, a clean clone, or CI.** Phase 11 at **62 of 72**. |

**Reading order for a new session:** the newest handoff first, then
[../09-phase-plan.md](../09-phase-plan.md), then the current phase document.

A handoff whose title says "half" means the phase is deliberately unfinished — its own §1 says what
is missing and why. Do not read an unticked checklist box in the phase document as an oversight
without checking there first.
